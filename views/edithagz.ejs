<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <title>تعديل الحجز</title>
  <link href="https://fonts.googleapis.com/css2?family=Almarai:wght@400;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary: #006d77;
      --accent: #83c5be;
      --light-bg: #f4f6f8;
      --dark-gradient: linear-gradient(to bottom right, #0f172a, #1e293b);
      --glass-bg: rgba(255, 255, 255, 0.08);
      --glass-border: rgba(255, 255, 255, 0.2);
    }

    body {
      font-family: 'Almarai', sans-serif;
      margin: 0;
      background-color: var(--light-bg);
      color: #333;
      transition: background 0.3s ease, color 0.3s ease;
      direction: rtl;
    }

    body.dark {
      background: var(--dark-gradient);
      color: white;
    }

    nav {
      background-color: var(--primary);
      padding: 1rem 2rem;
      color: white;
      display: flex;
      flex-direction: row-reverse;
      justify-content: space-between;
      align-items: center;
      transition: background 0.3s;
    }
    body.dark nav a:hover {
      color: #00d1ff;
      text-decoration: underline;
    }
    body.dark nav {
      background-color: rgba(0, 0, 0, 0.4);
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.4);
    }

    nav h1 {
      margin: 0;
      font-size: 24px;
    }

    body.dark nav h1 {
      font-size: 1.8rem;
      color: #00d1ff;
    }

    nav a, nav button {
      color: white;
      margin-left: 1rem;
      text-decoration: none;
      font-weight: bold;
      background: none;
      border: none;
      cursor: pointer;
      font-family: inherit;
    }

    .container {
      padding: 2rem;
      transition: all 0.3s ease;
    }

    body.dark .container {
      background: var(--glass-bg);
      border: 1px solid var(--glass-border);
      border-radius: 16px;
      backdrop-filter: blur(10px);
      box-shadow: 0 0 32px rgba(0, 0, 0, 0.3);
    }

    h2 {
      margin-bottom: 1rem;
    }

    form {
      background: white;
      padding: 2rem;
      border-radius: 10px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      max-width: 600px;
      margin: auto;
    }

    body.dark form {
      background: var(--glass-bg);
      color: white;
      box-shadow: 0 0 32px rgba(0, 0, 0, 0.3);
    }

    label {
      display: block;
      margin-bottom: 0.5rem;
      font-weight: bold;
    }

    input[type="text"],
    input[type="date"],
    input[type="time"],
    select {
      width: 100%;
      padding: 0.5rem;
      margin-bottom: 1rem;
      border-radius: 6px;
      border: 1px solid #ccc;
      font-family: inherit;
    }

    body.dark input,
    body.dark select {
      background: rgba(255, 255, 255, 0.1);
      color: white;
      border: 1px solid #888;
    }

    button[type="submit"] {
      background-color: var(--accent);
      color: white;
      border: none;
      padding: 0.75rem 1.5rem;
      border-radius: 8px;
      font-size: 1rem;
      cursor: pointer;
    }

    body.dark button[type="submit"] {
      background-color: #00d1ff;
    }

    button[type="submit"]:hover {
      background-color: var(--primary);
    }

    body.dark button[type="submit"]:hover {
      background-color: #00b0d1;
    }
  </style>
</head>
<body>

  <nav>
    <h1>Dashboard</h1>
    <div>
      <a href="/dashboard">Dashboard</a>
      <a href="/fantasy">Fantasy</a>
      <button id="toggle-dark">🌙</button>
      <a href="/logout">Logout</a>
    </div>
  </nav>

  <div class="container">
    <h2>تعديل الحجز</h2>
    <form action="/edit/<%= hte._id %>" method="POST">
      <label for="title">اسم الحجز:</label>
      <input type="text" id="title" name="title" value="<%= hte.name %>" required>
      <label for="date">التاريخ:</label>
      <input type="date" id="date" name="date" value="<%= hte.date.toISOString().split('T')[0] %>" required>
      <label for="location">المكان:</label>
      <input type="text" id="location" name="location" value="<%= hte.location %>" required>
      <button type="submit">حفظ التعديلات</button>
    </form>
  </div>

  <script>
    const toggleBtn = document.getElementById('toggle-dark');
    const currentTheme = localStorage.getItem('theme');
    if (currentTheme === 'dark') {
      document.body.classList.add('dark');
      toggleBtn.textContent = '☀️';
    }
    toggleBtn.addEventListener('click', () => {
      document.body.classList.toggle('dark');
      const isDark = document.body.classList.contains('dark');
      toggleBtn.textContent = isDark ? '☀️' : '🌙';
      localStorage.setItem('theme', isDark ? 'dark' : 'light');
    });
  </script>

</body>
</html>
