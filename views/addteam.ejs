<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>إضافة فريق</title>
  <link href="https://fonts.googleapis.com/css2?family=Almarai:wght@400;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary: #006d77;
      --accent: #83c5be;
      --light-bg: #f4f6f8;
      --dark-gradient: linear-gradient(to bottom right, #0f172a, #1e293b);
      --glass-bg: rgba(255, 255, 255, 0.08);
      --glass-border: rgba(255, 255, 255, 0.2);
    }

    body {
      font-family: 'Almarai', sans-serif;
      margin: 0;
      background-color: var(--light-bg);
      color: #333;
      transition: background 0.3s ease, color 0.3s ease;
    }

    body.dark {
      background: var(--dark-gradient);
      color: white;
    }

    nav {
      background-color: var(--primary);
      padding: 1rem 2rem;
      color: white;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    body.dark nav {
      background-color: rgba(0, 0, 0, 0.4);
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.4);
    }

    nav a, nav button {
      color: white;
      margin-left: 1rem;
      text-decoration: none;
      font-weight: bold;
      background: none;
      border: none;
      cursor: pointer;
      font-family: inherit;
    }

    .container {
      max-width: 600px;
      margin: 2rem auto;
      background: white;
      padding: 2rem;
      border-radius: 16px;
      box-shadow: 0 0 16px rgba(0,0,0,0.1);
      transition: all 0.3s ease;
    }

    body.dark .container {
      background: var(--glass-bg);
      border: 1px solid var(--glass-border);
      backdrop-filter: blur(10px);
      box-shadow: 0 0 32px rgba(0, 0, 0, 0.3);
    }

    h1 {
      text-align: center;
      color: var(--primary);
    }

    body.dark h1 {
      color: #00d1ff;
    }

    label {
      display: block;
      margin-top: 1rem;
      font-weight: bold;
    }

    input[type="text"] {
      width: 100%;
      padding: 0.8rem;
      margin-top: 0.5rem;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-family: inherit;
    }

    #add-player-btn {
      margin-top: 1rem;
      background-color: var(--accent);
      color: white;
      padding: 0.5rem 1rem;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 14px;
    }

    #add-player-btn:hover {
      background-color: var(--primary);
    }

    button[type="submit"] {
      width: 100%;
      margin-top: 1.5rem;
      background-color: var(--accent);
      color: white;
      border: none;
      padding: 0.8rem;
      border-radius: 8px;
      cursor: pointer;
      font-size: 1rem;
      transition: background 0.3s;
    }

    button[type="submit"]:hover {
      background-color: var(--primary);
    }

    body.dark button[type="submit"] {
      background-color: #00d1ff;
    }

    body.dark button[type="submit"]:hover {
      background-color: #0096c7;
      box-shadow: 0 0 8px #00d1ff;
      transform: translateY(-2px);
    }
  </style>
</head>
<body>

  <nav>
    <h1>إضافة فريق</h1>
    <div>
      <a href="/dashboard">لوحة التحكم</a>
      <a href="/addHagz">إضافة حجز</a>
      <a href="/teams">الفرق</a>
      <button id="toggle-dark">🌙</button>
      <a href="/logout">تسجيل خروج</a>
    </div>
  </nav>

  <div class="container">
    <h1>إضافة فريق جديد</h1>
    <form action="/add-team/<%= hagz._id %>" method="POST">
      <label>اسم الفريق</label>
      <input type="text" name="teamName" placeholder="أدخل اسم الفريق" required>

      <label>أعضاء الفريق</label>
      <div id="players-container">
        <input type="text" name="members[]" placeholder="اسم اللاعب الأول" required>
      </div>
      <button type="button" id="add-player-btn">➕ إضافة لاعب</button>

      <button type="submit">💾 حفظ الفريق</button>
    </form>
  </div>

  <script>
    const toggleBtn = document.getElementById('toggle-dark');
    const currentTheme = localStorage.getItem('theme');

    if (currentTheme === 'dark') {
      document.body.classList.add('dark');
      toggleBtn.textContent = '☀️';
    }

    toggleBtn.addEventListener('click', () => {
      document.body.classList.toggle('dark');
      const isDark = document.body.classList.contains('dark');
      toggleBtn.textContent = isDark ? '☀️' : '🌙';
      localStorage.setItem('theme', isDark ? 'dark' : 'light');
    });

    const playersContainer = document.getElementById('players-container');
    const addPlayerBtn = document.getElementById('add-player-btn');

    addPlayerBtn.addEventListener('click', () => {
      const input = document.createElement('input');
      input.type = 'text';
      input.name = 'members[]';
      input.placeholder = 'اسم لاعب آخر';
      input.required = true;
      playersContainer.appendChild(input);
    });
  </script>

</body>
</html>
